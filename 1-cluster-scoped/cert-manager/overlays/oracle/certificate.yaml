apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: acemaster-wildcard
  namespace: cert-manager
spec:
  secretName: acemaster-shop-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - acemaster.shop
    - "*.acemaster.shop"
